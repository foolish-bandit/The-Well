# Data Schema

*Placeholder. This document will describe the YAML schema for judge records: field definitions, citation requirements, enumerations, and validation rules. Expect substantive content once the schema stabilizes.*

The canonical schema will live under `data/schema/` (JSON Schema), with this document serving as the human-readable reference.
